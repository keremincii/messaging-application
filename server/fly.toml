app = "messaging-app-server"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 8080

  [services.concurrency]
    type = "connections"
    hard_limit = 25
    soft_limit = 20
